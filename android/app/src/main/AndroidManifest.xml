<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Declared explicitly instead of relying only on plugin manifest merging
         (geolocator, image_picker, dio pull these in implicitly).
         La ubicación es opt-in por avistamiento (interruptor en
         sighting_form_screen.dart); si se deniega o el usuario la
         desactiva, se usa un punto al azar dentro de su ciudad. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- Sin READ_MEDIA_IMAGES: image_picker usa el selector de fotos del
         sistema (Photo Picker) para elegir una imagen de la galería, que
         no requiere ningún permiso de acceso a medios — así lo confirma
         el manifiesto del propio plugin (io.flutter.plugins.imagepicker),
         que no declara ni este permiso ni READ_EXTERNAL_STORAGE. Declararlo
         aparte era innecesario y es justo lo que activa la revisión de
         "Permisos de fotos y vídeos" de Play Console. -->

    <!-- litert-gpu-api (dependencia transitiva de nsfw_detector_flutter,
         para el delegado GPU de TFLite) no declara targetSdkVersion en su
         propio manifiesto, y el fusionador de Android inyecta estos tres
         permisos de forma heredada "por si acaso" cuando eso pasa — ver
         build/app/outputs/logs/manifest-merger-release-report.txt,
         "IMPLIED ... reason: org.tensorflow.lite.gpu.api has a
         targetSdkVersion < 4". El filtro NSFW es cómputo local puro, no
         toca almacenamiento; se retiran explícitamente. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" tools:node="remove"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" tools:node="remove"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE" tools:node="remove"/>

    <!-- Compatibilidad Android TV / tablets / pantallas sin táctil: Google
         Play asume por defecto que toda app requiere táctil
         (android.hardware.touchscreen) y, además, deriva de los permisos
         de arriba android.hardware.camera + .autofocus (por CAMERA) y
         android.hardware.location.gps/.network (por ACCESS_FINE/COARSE_
         LOCATION, solo si targetSdk<=20 — aquí no aplica, pero se declara
         explícito igualmente). Sin esto, Play filtra la ficha de Play Store
         y la app queda "no disponible para tu dispositivo" en cualquier
         Android TV/Google TV, exactamente el síntoma reportado en un
         MediaTek AndroidTV: "This item is not compatible with your
         device" / "No eligible devices for app install".
         Ninguna de estas features es imprescindible: la foto del
         avistamiento es opcional (ver sighting_form_screen.dart), y la
         ubicación ya cae a un punto aleatorio dentro de la ciudad del
         perfil si no hay GPS (randomPointNear, ver sighting_geocoding.dart)
         — por eso todas van con required="false", nunca "true". -->
    <uses-feature android:name="android.hardware.touchscreen" android:required="false"/>
    <uses-feature android:name="android.hardware.faketouch" android:required="false"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-feature android:name="android.hardware.camera.any" android:required="false"/>
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false"/>
    <uses-feature android:name="android.hardware.location" android:required="false"/>
    <uses-feature android:name="android.hardware.location.gps" android:required="false"/>
    <uses-feature android:name="android.hardware.location.network" android:required="false"/>
    <uses-feature android:name="android.hardware.microphone" android:required="false"/>
    <uses-feature android:name="android.hardware.telephony" android:required="false"/>
    <uses-feature android:name="android.hardware.sensor.accelerometer" android:required="false"/>
    <uses-feature android:name="android.hardware.sensor.gyroscope" android:required="false"/>
    <uses-feature android:name="android.hardware.nfc" android:required="false"/>
    <uses-feature android:name="android.hardware.bluetooth" android:required="false"/>
    <uses-feature android:name="android.hardware.wifi" android:required="false"/>

    <application
        android:label="${appLabel}"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- App ID real de AdMob (banner de anuncios, ver
             lib/widgets/ad_banner.dart y lib/core/utils/constants.dart) —
             no es el ID de prueba público de Google
             (ca-app-pub-3940256099942544~3347511713). Sin este meta-data
             el SDK de AdMob crashea al arrancar aunque los anuncios estén
             deshabilitados por AppConstants.adsEnabled. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-2233716909370832~1446591370"/>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
